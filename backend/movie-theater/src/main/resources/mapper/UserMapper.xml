<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.my.movietheater.user.mapper.UserMapper">

    <!-- =========================
         로그인: 이메일로 사용자 조회
         (비밀번호 비교는 Service에서)
    ========================= -->
    <select id="findByEmail"
            resultType="com.my.movietheater.user.dto.UserDto">
        SELECT
            user_id     AS userId,
            email,
            password,
            name,
            role
        FROM users
        WHERE email = #{email}
    </select>

    <!-- =========================
         이메일 중복 체크
    ========================= -->
    <select id="countByEmail" resultType="int">
        SELECT COUNT(*)
        FROM users
        WHERE email = #{email}
    </select>

    <!-- =========================
         회원 생성 (USER)
         user_id : auto_increment
    ========================= -->
    <insert id="insertUser"
            useGeneratedKeys="true"
            keyProperty="userId">
        INSERT INTO users (
            email,
            password,
            name,
            phone,
            role
        ) VALUES (
            #{email},
            #{password},
            #{name},
            #{phone},
            #{role}
        )
    </insert>

</mapper>
