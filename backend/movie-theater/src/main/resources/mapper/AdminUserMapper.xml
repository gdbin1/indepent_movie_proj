<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.my.movietheater.admin.user.mapper.AdminUserMapper">

    <!-- =========================
         ADMIN 회원 목록 조회
    ========================= -->
    <select id="selectUserList" resultType="com.my.movietheater.admin.user.dto.AdminUserDto">
        SELECT
            user_id     AS userId,
            email,
            name,
            phone,
            role,
            created_at AS createdAt
        FROM users
        ORDER BY created_at DESC
    </select>

    <!-- =========================
         회원 권한 변경
    ========================= -->
    <update id="updateUserRole">
        UPDATE users
        SET role = #{role}
        WHERE user_id = #{userId}
    </update>

</mapper>
